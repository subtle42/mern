export default {
    db: {
        mongoose: {
            app: {
                host: "localhost",
                dbname: "merntest",
                port: 27017
            }
        }
    },
    shared: {
        secret: "dantheman"
    }
};