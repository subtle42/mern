import 'mocha'

describe('fronend test', () => {
    // let service:MainConent;

    beforeEach(() => {
        // service = new MainConent({})
    })

    it('should run a test', () => {
        // expect(service.handleSelect()).to.equal(1);
    })
})
